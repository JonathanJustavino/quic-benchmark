{
  "name": "quic-benchmark",
  "version": "1.0.0",
  "description": "benchmarking quic",
  "main": "tls-server.js",
  "scripts": {
    "start": "npm run build && node ./lib/tls-server.js",
    "build": "babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "me",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "@babel/preset-env": "^7.12.1"
  }
}
